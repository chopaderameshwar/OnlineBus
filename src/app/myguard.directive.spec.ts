import { MyguardDirective } from './myguard.directive';

describe('MyguardDirective', () => {
  it('should create an instance', () => {
    const directive = new MyguardDirective();
    expect(directive).toBeTruthy();
  });
});
